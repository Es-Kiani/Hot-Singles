#include <iostream>

using namespace std;

struct fooBar
{
    const fName, lName;
    int age;
};

int main()
{
    int x, y, z = 0;

    for (i; i < z; i++)
    {
        x=5;
	p = -2.85;
        ~! This is a comment...
        x++;
        if (4 >= 9 && 0 == 0.0)
	    continue;
	else
	    break;       
    }

    auto hello = true, buyBuy=false;

    

    return 0;
}

eof