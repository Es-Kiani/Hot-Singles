#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int x, y;

    x = pow(2, 3);
    y = sqrt(25);
    
    cout << x << endl
         << y;

    return 0;
}